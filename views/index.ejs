<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Foglalási űrlap</title>
  <link
  href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
  rel="stylesheet"
/>

</head>
<body>
  <div class="container">
    <div class="col-md-8 col-lg-5 mx-auto">
      <div class="card p-4">
        <h2 class="text-center mb-4">Foglalási adatok</h2>

        <form method="post" action="/reserv">
          <div class="mb-3">
            <label for="name" class="form-label">Név</label>
            <input
              type="text"
              id="name"
              name="name"
              class="form-control"
              required
              value="<%= values ? values.name : '' %>"
            />
          </div>

          <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input
              type="email"
              id="email"
              name="email"
              class="form-control"
              required
              value="<%= values ? values.email : '' %>"
            />
          </div>

          <div class="mb-3">
            <label for="date" class="form-label">Időpont</label>
            <input
              type="date"
              id="date"
              name="date"
              class="form-control"
              required
              value="<%= values ? values.date : '' %>"
            />
          </div>

          <div class="mb-3">
            <label for="customers" class="form-label">Vendégek száma</label>
            <input
              type="number"
              id="customers"
              name="customers"
              class="form-control"
              required
              min="1"
              value="<%= values ? values.customers : '' %>"
            />
          </div>

          <% if (error && error.length > 0) { %>
            <div class="alert alert-danger text-center">
              <%= error %>
            </div>
          <% } %>

          <div class="d-grid mt-4">
            <button type="submit" class="btn-modern">Foglalás</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <link rel="stylesheet" href="/style.css" />
</body>
</html>
